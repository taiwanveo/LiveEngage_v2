# le-participant.zeabur.app → le-join.zeabur.app（保留舊 QR／書籤）
# 注意：SPA hash（#/join/…）無法靠 HTTP 301 傳遞，故以 HTML+JS 永久導向並保留 fragment。

FROM docker.io/zeabur/caddy-static:latest

COPY frontend/redirect-participant/ /usr/share/caddy/
